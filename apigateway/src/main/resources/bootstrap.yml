# bootstrap 컨텍스트: application.yml보다 먼저 떠서 config-service의 설정을 가져온다.
# spring.application.name이 여기 있어야 한다 — {name}.yml 조회 키로 쓰이기 때문이다.
spring:
  application:
    name: apigateway
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      fail-fast: true
