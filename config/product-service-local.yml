spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/product_service?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8"
    username: cpuser
    password: cppass

  kafka:
    bootstrap-servers: localhost:9092,localhost:9192,localhost:9292

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
