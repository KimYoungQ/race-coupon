# JWT 서명/검증 공유 시크릿 (HS256, 최소 32바이트).
# apigateway와 user-service가 같은 값을 써야 한다.
#
# 이 파일의 키는 하나뿐이다. DB 접속·Kafka 주소·포트 등 나머지 값은
# config/*.yml과 docker-compose.yml에 직접 적혀 있다.
JWT_SECRET=change_me_to_at_least_32_bytes_secret
