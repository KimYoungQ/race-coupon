server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    # Eureka 서버 자신은 등록/조회 대상이 아니다. 끄지 않으면 자기 자신에 등록 시도 로그가 도배된다.
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    root: INFO
